spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kanboard_dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: INFO
    com.taskmanager.kanboard: DEBUG
